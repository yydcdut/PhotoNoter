sudo: false
language: android
jdk:
  - oraclejdk8

android:
    components:
      - build-tools-23.0.1
      - android-23

before_script:
  - chmod +x gradlew

script:
    - TERM=dumb ./gradlew check

